 Purpose:
Pretty Dashboards, for easy and quick monitoring of hardware

Access Information:
- URL: http://mntr.lab.local
- Location (VM): mntr.lab.local
- Container/Service Name: grafana (Docker Container)

Credentials:
- Admin Username: jireland
- Admin Password Location: [in password manager]

Technical Details:
- Database: n/a
- Configuration Files: /opt/docker/docker-compose.yml
- Data Location: /var/lib/docker/volumes/docker_grafana-storage/_data
- Log Location: n/a

Common Operations:

Restart:
docker compose restart grafana

View Logs:
docker logs grafana

Check Status:
docker ps | grafana

Backup:
docker compose stop grafana
sudo tar -czf /opt/backups/grafana-$(date +%Y%m%d).tar.gz
/var/lib/docker/volumes/grafana-data/_data/
docker compose start grafana

Dependencies:
- Requires: FreeIPA (DNS), Prometheus (metrics), Loki (logs)
- Required by: n/a

Notes:
- 
